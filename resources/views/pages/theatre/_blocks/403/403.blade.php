<section class="not-found">
  <div class="not-found__inner">
    <h2>403</h2>
    <p>{{ __('home.unauthorized_action') }}</p>
    <a href="{{ url()->previous() }}" class="not-found__link">{{ __('home.return_back') }}</a>
  </div>
</section>
